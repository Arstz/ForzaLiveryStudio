Small changes:

Medium changes:
    -Fit SVG stroke objects with the Lining fitter along their centreline and pen width instead of filling the stroked outline (Import Image as Shapes and SVG Bucket): thin rings and bands would use the Arc and Pill catalog instead of triangle meshes

Big changes:
    -Move header setting to a proper export menu
    -Color aware point snapping for Pen contours

Manual tasks:
    -Create splash screen image