Small changes:
    -Do not create a folder if project failed to export
    -Automatically ungroup contour fills if they contain only 1 shape in group - group should have at least 2 children
    -Deprecate double check for car model LOD

Medium changes:
    -Some liveries still fail to be recognized by the game

Big changes:
    -Add tuning details support for liveries

Manual tasks:
    -Create splash screen image and file association icon